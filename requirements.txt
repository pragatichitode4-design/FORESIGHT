pandas
numpy
scikit-learn
joblib
matplotlib
plotly
streamlit>=1.36
fastapi
uvicorn
jupyter
pytest
python-dateutil

# Additional forecasting models (per mentor guidance) — used by
# src/model_comparison.py. If any of these fail to install, the
# comparison script still runs and simply skips that model with a
# clear message rather than crashing.
xgboost
lightgbm
prophet
statsmodels
